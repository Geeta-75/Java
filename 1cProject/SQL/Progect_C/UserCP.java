package Progect_C;

public class UserCP{
    private CProvider cProvider;
    private DBManager dbManager;

    public UserCP(CProvider cProvider, DBManager dbManager) {
        this.cProvider = cProvider;
        this.dbManager = dbManager;
    }

    // Methods to interact with the course provider
}
